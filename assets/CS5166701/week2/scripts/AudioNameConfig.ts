namespace GameAudio {
     export enum Effect {
        Button = 'button',
        Step = 'step',
        Jump = 'jump',
    }

    export enum Music {
        BGM = 'bgm',
    }
}

export default GameAudio;

